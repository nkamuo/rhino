
type User {
    id: Ulid
    firstName: String
    lastName: String
    email: String
    phone: String
}


type UserConnection {
    totalCount: Int
    pageInfo: PageInfo
    edges: [UserEdge!]
}

type UserEdge {
    node: User
    cursor: String
}